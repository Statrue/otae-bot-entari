# 未完成工作总结

> 生成日期：2026-07-27

---

## 一、关卡查询（Stage Query）— Phase 1-4 已完成，等待审核合并

Phase 1（常驻关卡）、Phase 2（危机合约）、Phase 3（战争回响）、Phase 4（影拓丰碑）代码已实现，统一复用 `/zmd 关卡`、`/zmd 副本` 入口：

| 模块 | 文件 | 状态 |
|---|---|---|
| 领域模型 | `plugins/endfield/stage_models.py` | 新文件，未跟踪 |
| FZ Wiki 适配器 | `plugins/endfield/stage_source.py` | 新文件，未跟踪 |
| AkeData 适配器 | `plugins/endfield/akedata_stage_source.py` | 新文件，未跟踪 |
| 搜索与选择服务 | `plugins/endfield/stage_service.py` | 新文件，未跟踪 |
| 图片卡渲染 | `plugins/endfield/stage_draw.py` | 新文件，未跟踪 |
| 命令入口 | `plugins/endfield/commands.py` | 已修改，未暂存 |
| 插件集成 | `plugins/endfield/__init__.py` | 已修改，未暂存 |
| 数据源声明 | `plugins/endfield/sources.py` | 已修改，未暂存 |
| HTTP 客户端 | `plugins/endfield/client.py` | 已修改，未暂存 |
| 测试 | `tests/test_endfield_stage.py` | 新文件，未跟踪 |

`tests/test_endfield_stage.py` 已覆盖命令解析、数据适配、搜索选择、图片卡渲染、分页、三态语义、敌人抗性与失衡值，以及 Phase 2-4 类型化扩展。

实时 FZ Wiki 验证结果：

- 危机合约：3 个配置、46 个指标、等级阈值 `(25, 35, 81)`、3 个任务组、1 个商店。
- 战争回响：每关普通/困难/残酷 3 个变体、3 期轮换、满星 9 星。
- 实时图片已生成到 `.runtime/endfield_stage_phase23/`：`危机合约_detail.png`、`野性旧事_detail.png`、`catalog_1.png`。
- 非首个战争回响条目“战争简史”已验证为 `活动/战争回响/谵妄赛季#indie_battletower008`，并正确渲染 3 个变体。
- AkeData Manifest 当前版本为 `1.4.4@8764515-7`，影拓丰碑共 6 个系列、25 个关卡、普通/苦难共 50 个变体。
- 实时默认查询“撼山雾火”已确认选择 AkeData 候选；普通推荐 Lv.60、苦难推荐 Lv.90，敌人、机制与 5 项首通奖励均正确解析。
- 丰碑详情卡与目录卡已生成到 `.runtime/akedata/` 并完成目视检查。

本轮另修复了危机合约敌人聚合崩溃、波次/敌人合并、`baseAttrs` 数值读取、波次敌人覆盖，以及抗性/失衡矩阵显示；新增 AkeData Manifest 动态版本解析、默认目录合并与候选来源透传；`CARD_RENDER_VERSION` 已升到 `endfield-card-v36`。

最终验证已完成：关卡定向测试 62 项、Endfield/账号/性能/视觉回归 251 项，共 313 项通过；相关模块 `py_compile`、帮助图片 `--check-only` 和 `git diff --check` 均通过。实时 AkeData 数据与图片检查同样完成。

### 2026-07-27 霜冻联结敌人属性补正

复核用户提供的 AkeData 截图后，确认原实现只显示 `EnemyAttributeTemplateTable` 等级模板值，未应用敌人实例 `attrModifiers` 与场景 `SpawnerConfig.enemyLibrary[].bornBuffList`。现已补齐公共资源加载、Buff Blackboard 覆盖和 AkeData 八阶段属性公式：

- “霜冻联结·苦难”破潮之像 Lv.90：`917164 × 1.5 × 1.8 = 2476342.8`，攻击 `4130`、防御 `100`。
- “霜冻联结·普通”破潮之像 Lv.60：实例最大生命 `+50%`、刷怪 Buff 最大生命 `+50%`，最终生命 `556884`。
- 失衡数据改为读取 `levelIndependentAttributes`：破潮之像显示失衡值上限 `980`、恢复时间 `10s`、处决承伤系数 `1.50`；出生 Buff 的 `PoiseRecTimeScalar +80%` 独立显示为失衡恢复时间系数 `1.80`，不会把基础恢复时间误算成 `18s`。
- 空的 `poiseKnotPctList` 不再误显示“失衡节点：无”；`CARD_RENDER_VERSION` 已升到 `endfield-card-v36`，实时卡片生成于 `.runtime/akedata_frost_fix/frost_link_hard.png`。
- 关卡定向测试现为 62 项，Endfield/账号/性能/视觉回归 251 项，共 313 项通过；相关模块 `py_compile` 与 `git diff --check` 通过。

**下一步**：进行代码审查；确认后仅提交关卡相关文件。

---

## 二、账号详情 UI（Account Detail）— 代码已完成，等待审核合并

| 模块 | 文件 | 状态 |
|---|---|---|
| 领域模型 | `plugins/endfield/account_detail_models.py` | 新文件，未跟踪 |
| 服务层 | `plugins/endfield/account_detail_service.py` | 新文件，未跟踪 |
| 图片渲染 | `plugins/endfield/account_detail_draw.py` | 新文件，未跟踪 |
| 账号列表 | `plugins/endfield/account_draw.py` | 新文件，未跟踪 |
| 账号 UI 模型 | `plugins/endfield/account_models.py` | 新文件，未跟踪 |
| 插件集成 | `plugins/endfield/__init__.py` | 已修改，未暂存 |
| 测试 | `tests/test_endfield_account_ui.py` | 新文件，未跟踪 |

新增 `/zmd 账号 [编号]` 命令，展示干员配装总览（干员、装备、武器、技能等级、潜能）。

**下一步**：代码审查，确认后端 API（`account_client.py`）返回数据兼容。

---

## 三、账号 UI 复刻（Account UI Replica）— 部分完成

规格文档已写：`docs/endfield_account_ui_replica_spec.md`

目标：基于游戏截图复刻三套 HTML/CSS 模板（个人名片、影拓丰碑、危机合约结算页），可输出 1920×1080 PNG。

| 模块 | 状态 |
|---|---|
| 个人名片 | `account_models.py` + `account_draw.py` 存在，待验证 |
| 影拓丰碑 | 规格文档完成，代码待确认 |
| 危机合约 | 规格文档完成，代码待确认 |
| UI 素材目录 | `plugins/endfield/assets/ui/` 已建立，内容待补充 |

---

## 四、UI 素材提取（UI Asset Extraction）— 工具链就绪

`docs/endfield_ui_asset_extraction.md` 详细记录了从本机游戏文件提取 Unity UI Texture2D 为 PNG 的完整流程。

| 脚本/工具 | 状态 |
|---|---|
| `scripts/extract_endfield_ui_bundles.py` | 新文件，未跟踪 |
| `scripts/export_endfield_ui_images.ps1` | 新文件，未跟踪 |
| `scripts/build_endfield_icon_catalog.py` | 新文件，未跟踪 |
| `scripts/patches/endfield-studio-live-v3.patch` | 新文件，未跟踪 |

需在开发机上手动执行提取流程。

---

## 五、寻访批量脚本（Gacha Batch Scripts）— 代码完成

| 脚本 | 用途 | 状态 |
|---|---|---|
| `scripts/export_endfield_gacha_history.py` | 批量导出已授权用户的完整寻访历史到 JSON | 新文件，未跟踪 |
| `scripts/aggregate_endfield_gacha_stats.py` | 聚合实测出率并与官方概率公示对比 | 新文件，未跟踪 |
| `docs/endfield_gacha_batch_scripts.md` | 使用说明文档 | 新文件，未跟踪 |

**待处理**：`output/` 目录未在 `.gitignore` 中忽略，如果入库需追加。

---

## 六、现有功能的待提交改动

以下 **已跟踪文件** 有未暂存修改，内容与上述新功能关联：

| 文件 | 改动说明 |
|---|---|
| `plugins/endfield/__init__.py` | 集成关卡查询、账号详情；`CARD_RENDER_VERSION` 升到 v36；缓存支持多图返回；`accounts` 命令支持编号选择 |
| `plugins/endfield/commands.py` | 增加 `关卡/副本` 别名；`/zmd 账号 [编号]` 支持选择器；帮助文本更新 |
| `plugins/endfield/draw.py` | `is_height_limit_error` 抽离为公用；抽卡分析展示优化（期望值、付费与免费区分） |
| `plugins/endfield/gacha.py` | 六星期望值区分免费抽；`SixStarExpectation` 增加合計字段 |
| `plugins/endfield/account_client.py` | 修改（关联账号详情 API） |
| `plugins/endfield/client.py` | 新增 FZ article/category 请求 |
| `plugins/endfield/sources.py` | 声明 FZ 支持 `stage` |
| `scripts/help_pages.json` | 帮助图片资源更新 |
| `tests/test_endfield.py` | 新语法对应的测试断言更新 |
| `tests/test_endfield_account.py` | 账号详情命令、候选解析测试 |
| `tests/test_endfield_performance.py` | 小幅修改 |
| `tests/test_endfield_visual.py` | 视觉回归样本扩充 |

**注意**：这些改动与 untracked 新文件属于同一功能批次，建议一起提交。

---

## 七、未开始的工作

1. **森空岛公开数据查询** — `scripts/query_skland_endfield.py` 已存在但未在测试或 bot 中集成
2. **UI 素材提取实际操作** — 工具链就绪但未在开发机运行
3. **`output/` 目录 `.gitignore` 配置** — 如果寻访导出数据不应入库

## 八、文档清单

| 文档 | 说明 | 状态 |
|---|---|---|
| `CONTEXT.md` | 终末地插件领域语言 | 新文件，未跟踪 |
| `docs/adr/0001-model-stage-difficulties-as-variants.md` | ADR：难度为关卡变体 | 新文件，未跟踪 |
| `docs/adr/0002-use-typed-stage-family-extensions.md` | ADR：类型化玩法扩展 | 新文件，未跟踪 |
| `docs/endfield_stage_query_plan.md` | 关卡查询实施计划 | 新文件，未跟踪 |
| `docs/endfield_account_ui_replica_spec.md` | 账号 UI 复刻规格 | 新文件，未跟踪 |
| `docs/endfield_ui_asset_extraction.md` | UI 素材提取流程 | 新文件，未跟踪 |
| `docs/endfield_gacha_batch_scripts.md` | 寻访批量脚本说明 | 新文件，未跟踪 |
| `docs/skland_endfield_personal_api.md` | 森空岛个人数据接口笔记 | 新文件，未跟踪 |
| `docs/skland_endfield_public_query.md` | 森空岛公开数据查询笔记 | 新文件，未跟踪 |
| `docs/skland_endfield_ui_data_inventory.md` | UI 数据清单 | 新文件，未跟踪 |
